#include "main.h"

/**
* _abs - prints the absolute value of a number
* @n: expected parameter to be tested
* Return: the absolute value of nnđ
*
*/

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (n + n - n);
	}
}
